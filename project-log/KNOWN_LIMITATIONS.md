# Known Limitations

- Chưa có database, authentication, CMS hoặc feature application; các phần này thuộc task sau.
- Route `/` hiện là trang preview design foundation, chưa phải landing page hoặc application shell hoàn chỉnh.
# Supabase chưa được khởi tạo

Tài liệu đã chọn Supabase làm backend platform, nhưng repository chưa cài package, chưa có project/credentials, schema, migration, Auth, Storage, RLS hoặc backup thực tế. Đây là trạng thái dự kiến sau task chỉ cập nhật Markdown.
