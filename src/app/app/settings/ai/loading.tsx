import { Card } from "@/components/ui";

export default function LoadingAISettings() {
  return (
    <Card
      className="min-h-80 animate-pulse"
      aria-label="Đang tải AI settings"
    />
  );
}
