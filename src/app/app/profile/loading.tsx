import { LoadingState } from "@/components/ui";

export default function ProfileLoading() {
  return <LoadingState title="Đang tải hồ sơ workspace" description="CoffeeHub đang đọc thông tin tài khoản và preference của bạn." />;
}
