import Link from "next/link";
import { requireAdmin } from "@/lib/supabase/auth";
import { deleteManagedAction, saveSettingAction } from "@/features/site-settings/site-settings.actions";
import { getSiteSetting, listManagedContent } from "@/features/site-settings/site-settings.repository";
import { DeleteManagedButton } from "@/features/site-settings/components/delete-managed-button";
import { SiteSettingForm } from "@/features/site-settings/components/site-setting-form";
export default async function SettingsPage({ searchParams }: { searchParams: Promise<{ saved?: string; error?: string }> }) { const user = await requireAdmin(); const [[[links, faqs], setting], query] = await Promise.all([Promise.all([listManagedContent(), getSiteSetting(user.id)]), searchParams]); return <main className="mx-auto max-w-6xl px-[var(--page-gutter)] py-10"><h1 className="text-3xl font-semibold">Cài đặt website</h1><p className="mt-2 text-foreground-secondary">Quản lý nhận diện, SEO, liên kết và câu hỏi thường gặp.</p>{query.saved ? <p role="status" className="mt-5 rounded-sm border border-success/40 bg-success/10 p-3 text-sm text-green-200">Thay đổi đã được lưu.</p> : null}<section className="mt-8"><h2 className="text-xl font-semibold">Nhận diện và SEO</h2><SiteSettingForm setting={setting} action={saveSettingAction} error={query.error} /></section><ManagedList title="Navigation, footer và social" kind="link" items={links.map(item => ({ id:item.id, label:item.label, meta:`${item.kind} · ${item.url} · ${item.status} · thứ tự ${item.displayOrder}` }))} /><ManagedList title="FAQ" kind="faq" items={faqs.map(item => ({ id:item.id, label:item.question, meta:`${item.status} · thứ tự ${item.displayOrder}` }))} /></main>; }
function ManagedList({ title, kind, items }: { title:string; kind:"link"|"faq"; items:Array<{id:string;label:string;meta:string}> }) { return <section className="mt-8"><div className="flex items-center justify-between gap-4"><h2 className="text-xl font-semibold">{title}</h2><Link href={`/admin/settings/${kind}/new`} className="rounded-sm bg-primary-control px-4 py-3 text-sm font-semibold text-white">Thêm mới</Link></div><div className="mt-3 grid gap-3">{items.length ? items.map(item => <article key={item.id} className="flex flex-col gap-3 rounded-lg border border-border bg-surface p-4 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><h3 className="font-semibold">{item.label}</h3><p className="break-words text-sm text-foreground-secondary">{item.meta}</p></div><div className="flex gap-2"><Link href={`/admin/settings/${kind}/${item.id}/edit`} className="inline-flex min-h-10 items-center rounded-sm border border-border-strong px-3.5 text-sm font-semibold">Sửa</Link><DeleteManagedButton action={deleteManagedAction.bind(null,kind,item.id)} /></div></article>) : <p className="rounded-lg border border-dashed border-border p-6 text-center text-foreground-secondary">Chưa có nội dung.</p>}</div></section>; }
