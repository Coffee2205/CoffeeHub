import { Button, Input } from "@/components/ui";
import type { MediaAsset, ProjectMedia } from "@/generated/prisma/client";
import { deleteProjectMediaAction, uploadProjectMediaAction } from "../project-media.actions";
import { DeleteMediaButton } from "./delete-media-button";
type Item = ProjectMedia & { mediaAsset: MediaAsset };
export function ProjectMediaManager({ projectId, media, error, saved }: { projectId: string; media: Item[]; error?: string; saved?: boolean }) {
  return <section className="mt-6 rounded-lg border border-border bg-surface p-5"><h2 className="text-xl font-semibold">Ảnh project</h2><p className="mt-1 text-sm text-foreground-secondary">JPEG, PNG hoặc WebP; tối đa 5 MB. Bucket private và draft không được public.</p>{error ? <p role="alert" className="mt-4 text-sm text-red-200">{error}</p> : null}{saved ? <p role="status" className="mt-4 text-sm text-green-200">Media đã được cập nhật.</p> : null}<form action={uploadProjectMediaAction.bind(null, projectId)} className="mt-5 grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm"><span>Ảnh</span><Input name="file" type="file" accept="image/jpeg,image/png,image/webp" required /></label><label className="grid gap-2 text-sm"><span>Alt text</span><Input name="altText" required maxLength={240} /></label><label className="grid gap-2 text-sm"><span>Vai trò</span><select name="kind" className="min-h-11 rounded-sm border border-border bg-background-secondary px-3"><option value="GALLERY">Gallery</option><option value="COVER">Cover</option></select></label><div className="self-end"><Button type="submit">Upload ảnh</Button></div></form><div className="mt-6 grid gap-3">{media.length ? media.map((item) => <article key={item.mediaAssetId} className="flex flex-col gap-3 rounded-sm border border-border p-3 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><p className="truncate font-medium">{item.mediaAsset.originalName}</p><p className="text-sm text-foreground-secondary">{item.kind} · {item.mediaAsset.mimeType} · {Math.ceil(item.mediaAsset.sizeBytes / 1024)} KB</p><p className="text-sm text-foreground-secondary">Alt: {item.mediaAsset.altText}</p></div><DeleteMediaButton action={deleteProjectMediaAction.bind(null, projectId, item.mediaAssetId)} /></article>) : <p className="text-sm text-foreground-secondary">Chưa có ảnh.</p>}</div></section>;
}
