import { Button, Input, Textarea } from "@/components/ui";
import type { Project } from "@/generated/prisma/client";
import { PROJECT_STATUSES } from "../project.schema";
import { ProjectTimelineFields } from "./project-timeline-fields";
export function ProjectForm({ action, project, error }: { action: (form: FormData) => void | Promise<void>; project?: Project; error?: string }) { const date = (v?: Date | null) => v?.toISOString().slice(0, 10) ?? ""; return <form action={action} className="grid gap-5 rounded-lg border border-border bg-surface p-5 md:grid-cols-2">{error ? <p role="alert" className="md:col-span-2 rounded-sm border border-error/40 bg-error/10 p-3 text-sm text-red-200">{error}</p> : null}<Field label="Tiêu đề"><Input name="title" required maxLength={180} defaultValue={project?.title} /></Field><Field label="Slug"><Input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" defaultValue={project?.slug} /></Field><Field label="Tóm tắt" wide><Textarea name="summary" required maxLength={320} defaultValue={project?.summary} /></Field><Field label="Mô tả" wide><Textarea name="description" required className="min-h-48" defaultValue={project?.description} /></Field><Field label="Vai trò"><Input name="role" defaultValue={project?.role ?? ""} /></Field><Field label="Công nghệ (phân cách bằng dấu phẩy)"><Input name="techStack" defaultValue={project?.techStack.join(", ")} /></Field><Field label="GitHub URL"><Input name="githubUrl" type="url" defaultValue={project?.githubUrl ?? ""} /></Field><Field label="Live URL"><Input name="liveUrl" type="url" defaultValue={project?.liveUrl ?? ""} /></Field><ProjectTimelineFields startedAt={date(project?.startedAt)} endedAt={date(project?.endedAt)} /><Field label="Trạng thái"><select name="status" defaultValue={project?.status ?? "DRAFT"} className="min-h-11 rounded-sm border border-border bg-background-secondary px-3.5">{PROJECT_STATUSES.map((status) => <option key={status}>{status}</option>)}</select></Field><Field label="Thứ tự"><Input name="displayOrder" type="number" min={0} step={1} defaultValue={project?.displayOrder ?? 0} /></Field><div className="md:col-span-2"><Button type="submit">{project ? "Lưu thay đổi" : "Tạo project"}</Button></div></form>; }
function Field({ label, children, wide }: { label: string; children: React.ReactNode; wide?: boolean }) { return <label className={wide ? "grid gap-2 md:col-span-2" : "grid gap-2"}><span className="text-sm font-medium">{label}</span>{children}</label>; }
